const path = require("path");

module.exports = {
    paths: {
      input: "site", // This must be a string
      output: "_site",
      data: "_data",
      notes: "../notes", // Path for external notes
      css: "css",
      baseURL: "/@jessefreeman/foam-eleventy-mvp.main/apps/code-server/proxy/8080",
    },
  };
  
